# Base URL of the backend API.
# In development, Vite proxies requests — set this to the backend's local address.
# In production, point this to your deployed backend (e.g. https://api.example.com).
VITE_API_BASE_URL=http://localhost:4000
